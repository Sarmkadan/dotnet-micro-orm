# ApiResponseExtensions

Provides extension methods for constructing and manipulating `ApiResponse`, `ApiResponse<T>`, and `ApiPagedResponse<T>` objects, including adding request identifiers, contextual data, JSON serialization, and pagination utilities.

## API

### `WithRequestId`

Adds a request identifier to an `ApiResponse` object.
